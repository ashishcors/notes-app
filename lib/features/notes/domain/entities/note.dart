class Note {
  final String noteId;
  final String title;
  final String message;
  final int color;
  final bool isPinned;

  Note(this.noteId, this.title, this.message, this.color, this.isPinned);
}
